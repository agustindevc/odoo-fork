<odoo>
    <!-- Vista de lista de tipos de propiedad -->
    <record id="view_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario para un tipo de propiedad -->
    <record id="view_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <header>
                    <button name="%(estate.action_estate_property_offer)d"
                            type="action"
                            class="oe_stat_button"
                            icon="fa-list">
                        <field name="offer_count" widget="statinfo" string="Offers"/>
                    </button>
                </header>
                <sheet>
                    <group>
                        <!-- Campo principal del tipo de propiedad -->
                        <field name="name" class="oe_title" nolabel="1" style="font-size:50px; font-weight:bold;"/>
                    </group>

                    <notebook>
                        <!-- Pestaña con las propiedades asociadas a este tipo -->
                        <page string="Properties">
                            <field name="property_ids">
                                <!-- Lista de propiedades relacionadas -->
                                <list>
                                    <field name="name"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción para abrir la gestión de tipos de propiedad -->
    <record id="action_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>