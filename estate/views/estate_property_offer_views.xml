<odoo>
    <!-- Acción para abrir la vista de ofertas de propiedad -->
    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[("property_type_id", "=", active_id)]</field>
    </record>

    <!-- Vista de lista de ofertas de propiedad -->
    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers"
                decoration-success="accepted_bool"
                decoration-danger="refused_bool">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <field name="accepted_bool" invisible="1"/>
                <field name="refused_bool" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario para una oferta de propiedad -->
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group string="Offers"
                        decoration-success="accepted_bool"
                        decoration-danger="refused_bool">
                        <field name="price"/>
                        <field name="partner_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
